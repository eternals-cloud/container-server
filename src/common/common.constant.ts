export const ServiceProperties = {
  name: 'Container',
  key: 'container_server',
  version: 'v1.0',
  description: 'A container management platform is a software solution that automates the management of containerized applications.',
};

export enum GenderEnum {
  MALE = 'male',
  FEMALE = 'female',
  OTHER = 'other',
}

export enum ActiveEnum {
  TRUE = 'true',
  FALSE = 'false',
  EMPTY = '',
}

export enum OrderEnum {
  DESC = 'desc',
  ASC = 'asc',
  EMPTY = '',
}

export enum ViewTypesEnum {
  VIEW = 'view',
  COURSE = 'course',
}

export const DEFAULT_DEVICE_SESSIONS = {
  web: 1,
  app: 1,
};
